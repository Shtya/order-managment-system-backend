import { EMPTY_TOOLING_PRESETS, ToolingPresets } from "./types";

/** Same shape as openai-tooling-presets.ts. Fill after you test Anthropic ids. */
export const anthropicToolingPresets: ToolingPresets = EMPTY_TOOLING_PRESETS;
