import { EMPTY_TOOLING_PRESETS, ToolingPresets } from "./types";

/**
 * Official OpenAI-compatible protocol stub.
 * Add a new file + registry line for a specific vendor (e.g. GLM) later.
 */
export const openaiCompatibleToolingPresets: ToolingPresets =
  EMPTY_TOOLING_PRESETS;
