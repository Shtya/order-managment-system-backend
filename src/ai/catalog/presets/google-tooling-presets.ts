import { EMPTY_TOOLING_PRESETS, ToolingPresets } from "./types";

/** Same shape as openai-tooling-presets.ts. Fill after you test Google ids. */
export const googleToolingPresets: ToolingPresets = EMPTY_TOOLING_PRESETS;
