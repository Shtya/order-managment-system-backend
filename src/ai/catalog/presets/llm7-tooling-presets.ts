import { EMPTY_TOOLING_PRESETS, ToolingPresets } from "./types";

/** Same shape as openai-tooling-presets.ts. Fill after you test LLM7 ids. */
export const llm7ToolingPresets: ToolingPresets = EMPTY_TOOLING_PRESETS;
