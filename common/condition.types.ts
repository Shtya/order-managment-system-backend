export enum ConditionLogic {
  AND = "AND",
  OR = "OR",
}

export enum ConditionOperator {
  EQ = "eq",
  NEQ = "neq",
  IN = "in",
  NOT_IN = "not_in",
  IS_NULL = "is_null",
  IS_NOT_NULL = "is_not_null",
  GTE = "gte",
  LTE = "lte",
}
